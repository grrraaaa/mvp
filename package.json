{
  "name": "sber-sbbol-demo",
  "private": true,
  "description": "SBBOL demo — Next.js frontend + FastAPI on Vercel",
  "dependencies": {
    "next": "^15.5.18"
  },
  "scripts": {
    "build:mobile": "npm run build --prefix \"web mobile\" && node scripts/copy-mobile.mjs",
    "build": "npm run build:mobile && npm run build --prefix frontend",
    "vercel-build": "npm run build:mobile && npm run build --prefix frontend"
  }
}
